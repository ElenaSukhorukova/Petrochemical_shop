<section class='col-7'>
  <% provide :page_title, t('page.new', model: i18n_model_name(@bank).downcase) %>
  <% currently_at %>

  <%= render partial: 'shared/go_back_button', locals: { path: banks_path } %>
  <h1 class='md-4'><%= t('page.new', model: i18n_model_name(@bank).downcase) %></h1>
  <%= render partial: 'form',  locals: { model: @bank, method: :post, 
   submit: t('submit.create', model: i18n_model_name(@bank).downcase) } %>
</section>
