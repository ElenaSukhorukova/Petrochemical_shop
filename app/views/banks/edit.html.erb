<section class='col-7'>
  <% provide :page_title, t('page.edit', model: i18n_model_name(@bank).downcase) %>
  <% currently_at %>

  <h1 class='md-4'><%= t('page.edit', model: i18n_model_name(@bank).downcase) %></h1>
  <%= render partial: 'form', locals: { model: @bank, method: :put, 
    submit: t('submit.edit', model: i18n_model_name(@bank).downcase), path: banks_path } %>
</section>
