<% provide :page_title, t('page.edit', model: i18n_model_name(@department).downcase) %>
<% currently_at %>

<section class='col-7'>
  <h1 class='md-4'><%= t('page.edit', model: i18n_model_name(@department).downcase) %></h1>
  <%= render partial: 'form',  locals: { model: @department, method: :put, 
    submit: t('submit.edit', model: i18n_model_name(@department).downcase) } %>
</section>
