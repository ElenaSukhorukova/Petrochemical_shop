<section class='col-7'>
  <% provide :page_title, t('page.edit', model: i18n_model_name(@product).downcase) %>
  <% currently_at %>

  <h1 class='md-4'><%= t('page.edit', model: i18n_model_name(@product).downcase) %></h1>
  <%= render partial: 'supply_managers/products/form', locals: { model: @product, method: :put, 
    submit: t('submit.edit', model: i18n_model_name(@product).downcase) } %>
</section>
